"""Experimental, OFFLINE-ONLY modules.

NOTHING in this package is imported by the production app (`app.py`) or by the
production model (`calibrated_elo_model.py`). It exists purely for offline
diagnostics (Phase 2B). Importing it has no effect on the live site.
"""
