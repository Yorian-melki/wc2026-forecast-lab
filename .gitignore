.venv/
__pycache__/
.pytest_cache/
*.pyc
outputs/*.csv
outputs/*.json
outputs/*.parquet
outputs/*.npz

# Secrets — never commit (project holds 5 live API keys)
.env
.env.*
!.env.example
*.secret
secrets/

# Raw API responses may contain account-bound identifiers
data/raw/provider_responses/

# Audit JSON/CSV may contain masked key fragments (first6..last6) — exclude from any
# public repo. Track curated audit *.md (no key fragments) deliberately if needed.
outputs/audit/*.json
outputs/audit/*.csv
outputs/audit/thestatsapi_*
data/live/thestatsapi_*.json
data/live/*odds*.json

# Internal launch/ops notes — not for public repo
outputs/launch/
app.py.bak_before_dashboard_hotfix
!outputs/audit/final_maturity_score_v6.json
!outputs/audit/final_maturity_score_v5.json

# Allowlist: audit artifacts the live dashboard (app.py) reads at runtime. Verified
# secret-clean (no key fragments) so the deployed Data Quality page renders instead of
# showing "Run: ..." placeholders. Keep this list in sync with the artifacts app.py reads.
!outputs/audit/final_maturity_score_v4.json
!outputs/audit/wc_historical_backtest.json
!outputs/audit/ml_validation_report.json
!outputs/audit/beta_bootstrap_ci.json
!outputs/audit/xg_adjustment_audit.json
!outputs/audit/xg_probability_delta.csv
!outputs/audit/ml_ensemble_integration_decision.json
!outputs/audit/ml_ensemble_probability_delta.csv
!outputs/audit/model_stack_final_decision.json
!outputs/audit/tournament_walkforward_validation.json
!outputs/audit/upset_robust_ml_weighting.json

# Backup / junk (never commit)
*.bak
*.bak_*
app.py.bak_*
data/*.bak
*_BACKUP_PRE_P0.csv
.DS_Store

# Internal AI session-handoff notes — kept locally, not part of the public repo.
/session_handoff/

# Streamlit secrets (never commit) — config.toml (theme) is safe to track
.streamlit/secrets.toml
!outputs/audit/competition_backtest.json
!outputs/audit/data_lineage_map.json
!outputs/audit/reviewer_attack_audit.json
!outputs/audit/artifact_consistency_audit.json
