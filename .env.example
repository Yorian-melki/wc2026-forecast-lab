# WC2026 lab — environment template. Copy to .env and fill values. NEVER commit .env.
# Live-data providers (only needed for `update_live` / provider refresh; the forecast
# pipeline and all validation are fully reproducible OFFLINE without these).
API_FOOTBALL_KEY=
API_FOOTBALL_HOST=v3.football.api-sports.io
THESTATSAPI_KEY=
HIGHLIGHTLY_API_KEY=
HIGHLIGHTLY_BASE_URL=
HIGHLIGHTLY_DOC_URL=
FOOTBALL_DATA_ORG_KEY=
THESPORTSDB_API_KEY=
PRIMARY_LIVE_PROVIDER=api_football
LIVE_REFRESH_SECONDS=900
LIVE_STRICT_SOURCE_LOG=1
ALLOW_UNOFFICIAL_SCRAPERS=0
